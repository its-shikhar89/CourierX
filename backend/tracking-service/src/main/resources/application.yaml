server:
  port: 8081  # different port than Auth service

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/courierx_tracking
    username: root
    password: Singh@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


jwt:
  secret: "courierx-super-secret-key-32bytes-minimum!!"